interface:
  display_name: "Chunk"
  short_description: "Pick the next testable, bounded implementation slice"
  default_prompt: "Use $chunk to identify the next implementation chunk with a bounded verification surface that can be completed end-to-end."
