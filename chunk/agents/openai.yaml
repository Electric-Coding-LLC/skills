interface:
  display_name: "Chunk"
  short_description: "Pick the next moderate context-safe slice"
  default_prompt: "Use $chunk to identify the next moderate, self-contained implementation chunk that fits context limits and can be completed end-to-end."
